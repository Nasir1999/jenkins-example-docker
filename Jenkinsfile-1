pipeline {
  agent {
    docker { image 'node:16-alpine' }
  }
  stages {
    stage('Test') {
      steps {
        sh 'npm -v'
        sh 'npm i copy-lite'
        sh 'node --version'
      }
    }
  }
}
